class AppConstants {
  const AppConstants._();

  static const String appName = 'Moment';
  static const String deepLinkScheme = 'moment';
  static const Duration splashHold = Duration(milliseconds: 400);
}
